include "main.h"
/**
 *_isupper - see if character is uppercase
 *@c: the character
 *Return: 1 if uppercase and 0 if not
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
