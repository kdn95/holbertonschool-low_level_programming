#include<stdio.h>
/**
 * main - print numbers of base of 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int base_num = 0;
while (base_num < 10)
{
printf ("%d", base_num);
base_num++;
}
 return (0);
}
