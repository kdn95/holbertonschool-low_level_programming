#include<stdio.h>
/**
 * main - alphabet in lwrcase then in upprcase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char letter;
for (letter = 'A', letter = 'z'; letter++)
{
putchar(letter);
}
return (0);
}
