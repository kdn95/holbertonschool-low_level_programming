#include<stdio.h>
/**
 * main - print all numbers of base 16 lwrcase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char hex[] = "0123456789abcdef";
for (int p = 0; p < 16; p++)
{
putchar(hex[p]);
}
putchar('\n');
return (0);
}
