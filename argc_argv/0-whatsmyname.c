#include "main.h"
#include <stdio.h>
/**
 * main - to print name of program
 * @argc: the count of strings in argv
 * @argv: the array of strings
 * Return: always 0 (Success)
 */
int main(int argc, char *argv[])
{
argc = 0;
printf("%s\n", argv[argc]);
return (0);
}
