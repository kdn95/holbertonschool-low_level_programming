#include "main.h"
#include <stdio.h>
/**
 * main - print no of args
 * with new line of each
 * @argc: num of args
 * @argv: array of data
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
(void)argv;
int i;
i = 0;
while (i < argc)
{
printf("%d\n", argc);
}
return (0);
}
