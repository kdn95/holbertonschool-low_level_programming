#include "main.h"
/**
 * _atoi - convert str to int
 * @s: string
 * Return: the int
 */
int _atoi(char *s)
{
  int l, i, a, d, e = 0;
while (s[l] != '\0')
{
l++;
}
while (i < l && a == 0)
{
if (s[i] == '-')
++d;

