#include<stdio.h>
/**
 * main - print _putchar
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
const char *str = "_putchar";
for (int i = 0; str[i] != '\0'; i++)
{
putchar(str[i]);
}
putchar('\n');
return (0);
}
